# ---------------------------------------------------------------------------
# Antidote
source $(brew --prefix)/opt/antidote/share/antidote/antidote.zsh
antidote load

# ---------------------------------------------------------------------------
# Startship Prompt
eval "$(starship init zsh)"

# ---------------------------------------------------------------------------
# FNM (replacement of NVM)
export FNM_COREPACK_ENABLED=true
eval "$(fnm env --use-on-cd)"

# ---------------------------------------------------------------------------
# Aliases

# Node/Bun package managers
alias p=pnpm
alias pa='pnpm add'
alias pad='pnpm add -D'
alias pb='pnpm build'
alias pd='pnpm dev'
alias pin='pnpm install'
alias pln='pnpm lint'
alias pui='pnpm up -i -L'
alias prm='pnpm remove'
alias px='pnpm dlx'
alias pt='pnpm test'
alias ptc='pnpm test --coverage'
alias pp='pnpm why'

alias bx='bunx'

alias yx="yarn dlx"

# ls
alias ls="lsd --group-dirs=first"
alias ll="ls -l --git '--date=+%Y-%m-%d %H:%M:%S'"
alias l="ll --blocks=permission,size,date,name"
alias la="l -A"

# Editors
alias c="codium ."
alias z="zed ."

# removes the -v flag in the oh-my-zsh git plugin
alias gc="git commit"

# restore the run-help command (aliased to man for some reason)
unalias run-help
autoload run-help

# ---------------------------------------------------------------------------
# Worktrunk Integration

if command -v wt >/dev/null 2>&1; then
  wts() {
    wt switch --no-cd "$1" --execute=herdr -- worktree open --focus --branch "$1"
  }

  wtsc() {
    if [[ $# -ge 2 ]]; then
      wt switch --create --no-cd "$1" --base "$2" --execute=herdr -- worktree open --focus --branch "$1"
    else
      wt switch --create --no-cd "$1" --execute=herdr -- worktree open --focus --branch "$1"
    fi
  }

  _wtsc_completion() {
    local branches
    branches=($(git branch --format='%(refname:short)' 2>/dev/null))
    _describe 'branch' branches
  }

  alias wtl="wt list"

  # Add autocompletion to wts* functions
  compdef _wtsc_completion wts
  compdef _wtsc_completion wtsc

  eval "$(command wt config shell init zsh)"
fi

# ---------------------------------------------------------------------------

# Use nvim over SSH, VSCodium in a local GUI session
if [[ -n "$SSH_CONNECTION" || -n "$SSH_TTY" || -n "$SSH_CLIENT" ]]; then
  export EDITOR=nvim
else
  export EDITOR="codium --wait"
fi
# The zsh heuristic will change the keybinds to vi if the EDITOR contains vi
# This resets the shell keybindis to emacs
bindkey -e

# Initialize custom functions
function_dir="$HOME/.zsh/functions"
fpath=("$function_dir" $fpath)
autoload -Uz "$function_dir"/*(N:t)

# Initialize zsh completions
autoload -Uz compinit && compinit


